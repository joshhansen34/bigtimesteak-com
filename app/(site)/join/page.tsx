import Placeholder from '@/components/Placeholder';

export const metadata = { title: 'Join — OSBTSC' };

export default function Join() {
  return <Placeholder title="Join the Club" description="Sign in with Google or Facebook to become a member and submit reviews." />;
}
