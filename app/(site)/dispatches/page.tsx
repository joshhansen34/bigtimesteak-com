import Placeholder from '@/components/Placeholder';

export const metadata = { title: 'Dispatches from the Field — OSBTSC' };

export default function Dispatches() {
  return <Placeholder title="Dispatches from the Field" description="Occasional field reports from the founding membership. Coming soon." />;
}
