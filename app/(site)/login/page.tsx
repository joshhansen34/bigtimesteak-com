import Placeholder from '@/components/Placeholder';

export const metadata = { title: 'Sign In — OSBTSC' };

export default function Login() {
  return <Placeholder title="Sign In" description="Sign in with Google or Facebook to access your account." />;
}
